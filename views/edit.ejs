<% layout('layout') -%>

<div class="max-w-md mx-auto bg-white p-6 rounded-xl shadow-lg">
    <h2 class="text-2xl md:text-3xl font-bold text-gray-800 mb-6">Edit Chat</h2>
    <p class="text-sm text-gray-600 mb-4">Chat ID: <%= chat._id %></p>
    <p class="text-sm text-gray-600 mb-4">
        From: <span class="font-semibold"><%= chat.from %></span> to 
        <span class="font-semibold"><%= chat.to %></span>
    </p>
    <form action="/chats/<%= chat._id %>?_method=PUT" method="POST" class="space-y-6">
        <div>
            <label for="msg" class="block text-sm font-medium text-gray-700">Message</label>
            <textarea name="msg" id="msg" required rows="4"
                      class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-300 focus:ring focus:ring-blue-200 transition duration-200"
                      ><%= chat.msg %></textarea>
        </div>
        <button type="submit"
                class="w-full bg-gradient-to-r from-blue-600 to-blue-700 text-white py-3 px-4 rounded-lg hover:from-blue-700 hover:to-blue-800 transition duration-300 shadow-lg">
            Update Chat
        </button>
    </form>
</div>